services:
  - type: web
  name: yt-lyrics-service
  env: python
  plan: free
  repo: https://github.com/YOUR_USERNAME/YOUR_REPO
  branch: main
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn -k uvicorn.workers.UvicornWorker app:app --bind 0.0.0.0
